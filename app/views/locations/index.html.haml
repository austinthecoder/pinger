#locations
  %h2 URLs

  %p
    = link_to "Add URL", new_location_path
    |
    = link_to "Email callbacks", email_callbacks_path
    |
    = link_to "Alerts", alerts_path

  - if locations.present?
    %table.table
      %thead
        %tr
          %th Title
          %th URL
          %th HTTP method
          %th Seconds
          %th
        %tbody
          - locations.each do |location|
            - present location do
              %tr{id: "url_#{id}"}
                %td= link_to title, path
                %td= url
                %td= http_method
                %td= seconds
                %td
                  = link_to 'edit', edit_path, title: 'Edit this URL'
                  |
                  = link_to 'remove', delete_path, title: 'Remove this URL'
  - else
    %p.empty No URLs found.

  = paginate locations